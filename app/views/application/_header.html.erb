<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="/">ReuseIdeas</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if @current_user %>
        <%= header_link_item('アイデア一覧', ideas_path) %>
        <%= header_link_item("#{@current_user.name}さん", mypage_path) %>
        <%= link_to("ログアウト", logout_path, { method: :delete, data: {confirm: "ログアウトしますか？"}, class: "nav-link"}) %>
      <% else %>
        <%= header_link_item('新規登録', new_user_path) %>
        <%= header_link_item('ログイン', '/login') %>
      <% end %>
    </ul>
    <form class="form-inline my-2 my-lg-0">
      <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
      <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
    </form>
  </div>
</nav>